# Models Directory

Place your `.onnx` (DirectML) or `.engine` (TensorRT) models here.

## Recommended Structure
- `models/yolo11n.onnx`
- `models/yolo11n.engine`

The `IDetector` interface supports loading models from any path, but this is the default search location.
